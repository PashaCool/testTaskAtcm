spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5434/testAtaccamaDb
    driverClassName: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    database-platform=org: hibernate.dialect.PostgreSQL9Dialect
    properties:
      hibernate:
        ddl-auto: none
        dialect: org.hibernate.dialect.PostgreSQL9Dialect
server:
  port: 8081